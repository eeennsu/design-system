// C-19 게이트 (3)·(8) 전용 스캔 대상. T-G1 이 만들고 T-N1 이 실제 dist 로 대체한다(plan v2 F-14).
// 소비자 앱 소스에 클래스를 쓰지 않은 상태에서 래퍼의 `@source "../dist"` 만으로
// 클래스가 생성되는지 보기 위한 파일이다. 런타임에서 import 하지 않는다.
export const gateStubClassName = "bg-brand";
export const gateStubClassNames = "bg-canvas bg-surface-muted bg-danger text-fg text-fg-muted text-sm text-md text-xl text-2xl rounded-md p-4 p-8 gap-4 mt-6";
