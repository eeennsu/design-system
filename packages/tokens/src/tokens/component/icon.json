{
  "$description": "아이콘 크기는 클래스가 아니라 lucide size prop(JS 숫자)으로 전달한다 — 20px 는 spacing 열거 밖이라 size-5 가 생성되지 않는다(plan v2 F-8).",
  "component": {
    "icon": {
      "size": {
        "sm": { "$type": "number", "$value": 16 },
        "md": { "$type": "number", "$value": 20 },
        "lg": { "$type": "number", "$value": 24 }
      }
    }
  }
}
